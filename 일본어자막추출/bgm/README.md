# bgm/

`extract_high_pitch_video.py`가 고음영상에 배경음을 입힐 때 쓰는 mp3 폴더.

이 폴더에 `.mp3` 파일을 넣어두면, `extract_high_pitch_video.py` 실행 시
(기본적으로 `--no-bgm`을 주지 않는 한) 이 중 하나를 무작위로 골라
고음영상 위에 배경음으로 믹스한다. 자세한 사용법은
`일본어자막추출/README.md`의 "고음영상 배경음 입히기" 항목 참고.

지금은 비어있음 — mp3를 여기 커밋하면 바로 사용 가능.
