# 이어서 작업할 것 — 새 세션 첫 메시지로 붙여넣기

## 0. 가장 먼저 할 일
방금 전 세션에서 `~/Desktop` 하위 전체(이직시스템, shift_alarm 포함)가 갑자기 "Operation not permitted"로 막혔었다(macOS Terminal의 데스크탑 폴더 TCC 권한이 도중에 풀린 것으로 추정). 터미널을 껐다 켠 뒤 재승인했을 것이므로, 먼저 아래로 접근이 정상인지 확인할 것:
```bash
ls "/Users/forrestdpark/Desktop/PDG/DailyHelloWorld_/shift_alarm/"
```
막혀 있으면 사용자에게 시스템 설정 → 개인정보 보호 및 보안 → 파일 및 폴더(또는 전체 디스크 접근 권한)에서 Terminal의 "데스크탑 폴더" 권한을 다시 확인해달라고 요청할 것.

## 1. shift_alarm.py — 이미 완료되어 디스크에 반영됐을 가능성이 높음 (git diff로 확인만 할 것)
경로: `/Users/forrestdpark/Desktop/PDG/DailyHelloWorld_/shift_alarm/shift_alarm.py`

Edit 도구 호출은 전부 성공 응답을 받았으나, 그 이후 권한 문제로 Read/cat 확인을 못 한 상태다. `git diff -- shift_alarm/shift_alarm.py`로 아래 두 가지가 실제로 반영됐는지 먼저 확인:

1. **손동주 주 1회 통화 리마인더 추가** — 완료:
   - `REMINDERS` 딕셔너리에 `"call_sondongju": {"label": "📞 손동주한테 전화하는 날", "enabled": True}` 추가(동찬 항목 바로 아래)
   - `CALL_SONDONGJU_ANCHOR = datetime.date(2026, 8, 5)`, `CALL_SONDONGJU_INTERVAL_DAYS = 7` 상수 추가
   - `_is_sondongju_call_day(d)` 함수 추가(동찬 함수와 동일 패턴)
   - `get_today_reminders()`에 손동주 체크 블록 추가
   - `get_today_reminder_title_tokens()`의 `call_tokens` 딕셔너리에 `"📞동주"` 추가
   - 상단/함수 docstring 주석에도 손동주 항목 설명 추가
   - **이 항목은 완료됐고 아직 커밋 전이다 (git diff에 보여야 정상).**

2. **날씨 아이콘 한자로 교체** — 완료:
   - `fetch_weather()` 안의 `icon = "🌧️" if rain >= 50 else "⛅" if rain >= 20 else "☀️"`를
     `icon = "雨" if rain >= 50 else "曇" if rain >= 20 else "晴"`로 교체함(비/흐림/맑음 순, 임계값은 그대로 50%/20%).
   - **이 항목도 완료됐고 아직 커밋 전이다.**

## 2. shift_alarm.py — 아직 코드 작성 전, 지금부터 구현해야 함

### 2-1. 근무 며칠째 표기 (G1/D2/휴3 등) + 색상
사용자 요청 원문: "G1 G2 D1 D2 이런식으로 표기, 휴일은 빨간색 글자로 휴1 이런식" + GY 숫자는 노란색.

**설계(이미 확정, 구현만 하면 됨):**
- `get_shift_for_date(schedule, d)`를 이용해 오늘과 같은 근무 코드가 며칠째 연속되는지 세는 함수 추가:
  ```python
  def _shift_block_day_number(schedule, d, shift):
      """d를 포함해 shift(수동 오버라이드 반영한 오늘 표시값)가 며칠째 연속인지(1부터) 반환.
      과거 날짜는 근무표 원본(get_shift_for_date) 기준으로 센다."""
      if shift not in ("Day", "Swing", "GY", "휴무"):
          return None
      count = 1
      cursor = d - datetime.timedelta(days=1)
      while get_shift_for_date(schedule, cursor) == shift:
          count += 1
          cursor -= datetime.timedelta(days=1)
      return count
  ```
- `_update_title()`(2085번째 줄 부근)에서 `current = self.config.get("current_shift")`로 effective shift를 구하고, `_shift_block_day_number(self.schedule, datetime.date.today(), current)`로 일차를 구해서 `code`(예: "G") 뒤에 붙인다 → `shift_text = f"{code}{day_num}"` 형태.
- **색상 적용은 `self.title = ...`(plain 문자열, 기존 로직 그대로 유지)로 먼저 설정한 뒤, 곧바로 `NSMutableAttributedString`을 만들어 숫자 부분에만 색을 입히고 `self._nsapp.nsstatusitem.setAttributedTitle_(attributed)`로 덮어써야 한다.** (rumps의 `title` setter가 `setTitle_()`을 호출하는데 이게 attributedTitle을 초기화시키므로, 반드시 plain title 설정 → attributedTitle 설정 순서를 지킬 것. `self._nsapp`는 `.run()` 시작 후에만 존재하므로 `_update_title()`이 timer/콜백에서만 불린다면 문제없음 — 이미 확인함.)
  - GY 숫자 → `NSColor.systemYellowColor()`
  - 휴무 숫자 → `NSColor.systemRedColor()`
  - D/S 숫자는 색 없음(기본색)
  - import 추가 필요: `from Foundation import NSMutableAttributedString, NSRange` / `from AppKit import NSColor, NSForegroundColorAttributeName` (파일 상단 import 구역에)
- **기술 검증 완료**: `/private/tmp/claude-501/-Users-forrestdpark/b9161bc8-619c-4435-9fc9-024ed3c63748/scratchpad/test_colored_title.py`에서 독립 rumps 앱으로 `setAttributedTitle_()` 방식이 정상 동작하는 것을 실측 확인함(attributedTitle readback에 `NSColor = "Catalog color: System systemYellowColor"`가 특정 range에만 걸린 것 확인, plain title()도 정상 유지됨). 이 패턴을 그대로 shift_alarm.py에 이식하면 됨.

### 2-2. 저장공간 숫자(💾14) 색상
사용자가 선택한 옵션: **경고 기준(5GB 이하)일 때만 빨간색**, 평소엔 기본색.
- 기존 `LOW_STORAGE_WARNING_GB = 5` 상수를 그대로 재사용해서 `self.storage_free_gb <= LOW_STORAGE_WARNING_GB`일 때만 `💾` 뒤 숫자에 `NSColor.systemRedColor()` 적용.
- 위 2-1과 같은 attributedTitle 안에 같이 넣어야 한다 — 타이틀 전체(`shift_text + storage + reminder_icons + weather_icon`)에 대해 attributedTitle을 한 번만 만들고, 그 안에서 여러 range(근무 며칠째 숫자, 저장공간 숫자)에 각각 색을 입히는 구조로 짤 것. `parts`를 조립하는 기존 `_update_title()` 로직(2085~2102번째 줄 부근)을 참고해서, 각 조각의 문자열 내 시작 위치(offset)를 계산하면서 attributedTitle을 만들면 된다.

### 2-3. 메뉴바 재시작 시 기존 프로세스 먼저 종료
사용자 요청: "맥메뉴바 재실행할 때는 기존 매뉴바를 종료하면 좋겠어."
- 기존 메모리(`feedback_shift_alarm_auto_restart.md`)에 이미 있는 재시작 절차(`launchctl kickstart -k gui/$(id -u)/com.shiftalarm.menubar` 실행 후 `ps aux | grep shift_alarm`으로 중복 프로세스 있으면 오래된 PID kill)를 **매번 재시작할 때마다 항상 자동으로 수행하도록** 확실히 할 것. `launchctl kickstart -k`는 이미 기존 프로세스를 죽이고 재시작하는 옵션이라 원칙적으로 중복이 안 생겨야 하는데, 그래도 stray 프로세스가 남는 사례가 있었다는 게 기존 메모리 내용이므로, 재시작 스크립트/절차에 "kickstart 후 중복 PID 확인·정리"를 명시적 단계로 넣어달라는 뜻으로 이해하면 됨. shift_alarm.py 자체 코드 변경이 필요한 요청이 아니라 **내(Claude)가 재시작할 때 따르는 절차**에 대한 요청일 가능성이 큼 — 필요하면 사용자에게 "코드 레벨에서 뭔가를 바꿔달라는 거냐, 아니면 내가 재시작 명령을 실행하는 방식을 말하는거냐" 확인할 것.

## 3. shift_alarm — 모바일 접근 (아직 방향 결정 전, 조사만 됨)
사용자 요청: "복잡한 영상 작업 같은 거 제외하고 기초적인 리마인더나 날씨는 핸드폰에서도 확인하고 싶다."
아직 구현 방식을 사용자와 확정하지 않았다. 후보:
- Notion 동기화 (이미 ebook_reader.py 쪽에 Notion 토큰·연동이 있어서 재사용 가능 — `security find-generic-password -a "$USER" -s "ebook_reader_notion_token" -w`)
- iOS 단축어 + iCloud Drive에 상태 JSON 동기화 후 위젯으로 표시
- 웹페이지 하나 만들어서 Tailscale 등으로 폰에서 접속
방식을 사용자에게 먼저 물어보고 진행할 것 (AskUserQuestion 권장).

## 4. shift_alarm — 마무리 (2-1, 2-2, 2-3 구현 끝나면)
- README(`shift_alarm/README.md`)에 이번 변경사항(손동주 리마인더, 날씨 한자 아이콘, GY/D/S/휴 며칠째+색상 표기, 저장공간 색상) 전부 반영 — 기존 README는 "새 세션에서도 이 문서만 보고 이어서 작업할 수 있게" 유지하는 게 원칙이므로 날짜 붙여서 기록할 것(오늘 날짜 확인해서 사용).
- `git fetch && git log HEAD..origin/main --oneline`으로 원격에 다른 세션 커밋 없는지 확인 후, `shift_alarm.py`/`README.md`만 `git add`해서 커밋.
- 커밋 후 `launchctl kickstart -k gui/$(id -u)/com.shiftalarm.menubar`로 재시작하고 `ps aux | grep shift_alarm`으로 프로세스 1개만 있는지 확인.
- **주의**: `git push`는 사용자가 명시적으로 요청한 적 없으면 먼저 물어보고 할 것(지금까지 세션에서는 매번 커밋 후 바로 push까지 했었음 — 이번에도 같은 패턴이면 이어서 해도 됨, 애매하면 확인).

## 5. 이직시스템 — 워크넷(work24) API 통합 (진행 중, git diff로 어디까지 됐는지 먼저 확인)
경로: `/Users/forrestdpark/Desktop/PDG/DailyHelloWorld_/이직시스템/job_collector.py`

**배경**: 사람인 API는 개인도 신청 가능한데 아직 승인 대기 중(2026-08-03 신청, 아직 거절 확정 아님). 워크넷은 원래 openapi.work.go.kr였는데 **고용24(work24.go.kr)로 통합**됐고, 사용자가 직접 공식 개발명세서 표 전체를 찾아서 대화에 붙여넣어줬다(요청 URL·파라미터·응답 필드 전부 확보됨, 아래 정리).

**이미 코드에 반영된 것(git diff로 확인할 것, 안 됐으면 다시 넣을 것)**:
```python
import xml.etree.ElementTree as ET  # import 구역에 추가

SARAMIN_API_URL = "https://oapi.saramin.co.kr/job-search"   # 기존 API_URL을 이름만 바꿈
SARAMIN_MAX_RESULTS = 110
WORK24_API_URL = "https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo210L01.do"
WORK24_MAX_RESULTS = 100
WORK24_EMP_TP_NAMES = {
    "4": "파견근로",
    "10": "기간의 정함이 없는 근로계약",
    "11": "기간의 정함이 없는 근로계약(시간(선택)제)",
    "20": "기간의 정함이 있는 근로계약",
    "21": "기간의 정함이 있는 근로계약(시간(선택)제)",
    "Y": "대체인력채용",
}
```
`API_URL` 이름을 `SARAMIN_API_URL`로 바꿨으므로, 기존 `fetch_query()` 함수 안에서 `API_URL`을 참조하던 곳을 `SARAMIN_API_URL`로 고쳐야 하는데 **이 부분은 아직 안 했을 수 있다 — 확인 필수** (import만 넣고 함수 본문 수정 전에 권한 문제가 터졌음).

**아직 구현 안 된 것 (여기부터 실제 작업)**:

1. `Job` dataclass에 `source: str = "사람인"` 필드 추가(필수 필드 4개 — source_id/title/company/url — 뒤 아무 자리나, 전부 기본값 있는 필드들 사이).

2. 함수 이름 명확화(선택이지만 권장): `fetch_query` → `fetch_saramin_query`, `parse_job` → `parse_saramin_job`. 바꾸면 `test_job_collector.py`의 `jc.parse_job(...)` 호출도 `jc.parse_saramin_job(...)`로 같이 고칠 것.

3. 신규 함수 `fetch_worknet_query(access_key, query, config)`:
   ```python
   def fetch_worknet_query(access_key: str, query: str, config: dict[str, Any]) -> list[Job]:
       params = {
           "authKey": access_key,
           "callTp": "L",
           "returnType": "XML",
           "startPage": 1,
           "display": min(int(config.get("results_per_query", 30)), WORK24_MAX_RESULTS),
           "keyword": query,
       }
       request = urllib.request.Request(
           f"{WORK24_API_URL}?{urllib.parse.urlencode(params)}",
           headers={"Accept": "application/xml", "User-Agent": USER_AGENT},
       )
       try:
           with urllib.request.urlopen(request, timeout=30) as response:
               body = response.read()
       except urllib.error.HTTPError as exc:
           detail = exc.read(1000).decode("utf-8", "replace")
           raise RuntimeError(f"워크넷 API HTTP {exc.code}: {detail}") from exc
       except urllib.error.URLError as exc:
           raise RuntimeError(f"워크넷 API 연결 실패: {exc.reason}") from exc

       try:
           root = ET.fromstring(body)
       except ET.ParseError as exc:
           raise RuntimeError(f"워크넷 API 응답 파싱 실패: {exc}") from exc

       error = root.findtext("error")
       if error:
           raise RuntimeError(f"워크넷 API 오류: {error}")

       return [parse_worknet_job(node, query, config) for node in root.findall("wanted")]
   ```
   (`<GO24><error>...</error></GO24>` 형태 에러 응답이 실제로 존재하는 것 curl로 확인했었음 — authKey 없이 호출하면 "authKey는 필수 입니다." 에러가 옴.)

4. 신규 함수 `parse_worknet_job(node, query, config)`:
   ```python
   def parse_worknet_job(node: ET.Element, query: str, config: dict[str, Any]) -> Job:
       def text(tag: str) -> str:
           return plain(node.findtext(tag))

       title = text("title")
       company = text("company")
       industry = text("indTpNm")
       combined = " ".join((title, company, industry))
       skills = detect_skills(combined)
       emp_tp_code = text("empTpCd")
       return Job(
           source="워크넷",
           source_id=text("wantedAuthNo"),
           title=title,
           company=company,
           url=text("wantedInfoUrl"),
           location=text("region"),
           experience=text("career"),
           education=text("maxEdubg") or text("minEdubg"),
           employment_type=WORK24_EMP_TP_NAMES.get(emp_tp_code, emp_tp_code),
           salary=text("sal") or " ~ ".join(filter(None, (text("minSal"), text("maxSal")))),
           posted_at=text("regDt"),
           deadline=text("closeDt"),
           keywords=industry,
           skills=", ".join(skills),
           score=score_job(combined, config),
           matched_query=query,
       )
   ```

5. `upsert_jobs()`를 `(source, source_id)` 기준으로 고칠 것 — 지금은 `WHERE source = '사람인' AND source_id = ?`로 하드코딩돼 있고 INSERT 컬럼 목록에 `source`가 없어서(DB 기본값 '사람인'에 의존) 사람인 말고 다른 소스를 넣으면 항상 '사람인'으로 잘못 저장된다. `SELECT id FROM jobs WHERE source = ? AND source_id = ?`로 바꾸고, INSERT/VALUES에 `source`, `:source` 명시적으로 추가할 것.

6. `collect()` 함수를 두 소스 다 선택적으로 처리하도록 재작성:
   - `SARAMIN_ACCESS_KEY`, `WORK24_ACCESS_KEY` 둘 다 없으면 `SystemExit`.
   - 각각 있으면 그 소스만 수집, 없으면 "⚠️ ...없음 — ...건너뜀" 출력하고 계속 진행.
   - `collected` 딕셔너리 키를 `(source, source_id)` 튜플로 바꿔야 함(소스 다르면 source_id 겹칠 수 있으므로).

7. `list_jobs()`에 `source` 컬럼도 SELECT/출력에 추가(여러 소스 섞여서 보이니까 어디서 온 공고인지 알아야 함).

8. `doctor()`에 `WORK24_ACCESS_KEY` 상태도 같이 출력.

## 6. 이직시스템 — 사람인 공개 검색결과 크롤러 (아직 코드 없음, robots.txt 검토는 끝남)

**결론**: 사람인은 크롤링해도 된다고 판단했고, 잡코리아는 하지 않기로 했다. 이유:
- 사람인 robots.txt(`https://www.saramin.co.kr/robots.txt`)는 검색결과 경로를 막지 않음. 실제로 `https://www.saramin.co.kr/zf_user/search/recruit?searchword=...` 를 curl로 가져와서 정적 HTML 안에 `item_recruit` 클래스 20개가 그대로 있는 것 확인함(JS 렌더링 없이 파싱 가능, 헤드리스 브라우저 불필요).
- 잡코리아 robots.txt(`https://www.jobkorea.co.kr/robots.txt`)는 `/Search/?stext=`를 **일반 크롤러 전체**(`User-agent: *`) 대상으로 명시적으로 Disallow 하고 있고, 별도로 ClaudeBot/anthropic-ai 등 AI 크롤러를 사이트 전체(`Disallow: /`, 홈페이지·채용목록 등 극히 일부만 Allow) 차단하고 있음. 이 방침을 어기고 스크래핑하지 않기로 사용자에게 이미 설명·확인 받음 — **잡코리아 스크래핑은 하지 않는다.**

**구현 방향(로그인/CAPTCHA 우회 없이 공개 검색 페이지만)**:
- 사람인 검색 URL 패턴: `https://www.saramin.co.kr/zf_user/search/recruit?searchword=<검색어>&recruitPage=<페이지>&recruitSort=relation&recruitPageCount=20`
- 표준 라이브러리만 쓰는 기존 프로젝트 원칙(`job_collector.py` 파일 docstring에 명시됨)을 지키려면 `html.parser` 표준 라이브러리로 파싱하거나, 최소한의 정규식으로 `item_recruit` 블록을 잘라내는 방식. (`requests`/`beautifulsoup4` 같은 외부 패키지를 새로 추가할지, 표준 라이브러리를 고수할지는 아직 결정 안 됨 — 기존 코드 스타일(`urllib`+표준 라이브러리만)을 따르는 게 일관성 있음.)
- User-Agent를 명확한 사설 식별자로 유지(기존 `USER_AGENT` 상수 재사용 가능), 요청 간 적절한 delay(예: 1~2초) 넣어서 과도한 요청 자제.
- source="사람인(크롤링)" 또는 기존 "사람인"과 구분되는 별도 source 값 부여할지 결정 필요(사용자에게 물어볼 것 — API 데이터랑 크롤링 데이터를 같은 소스로 취급할지, 구분할지).
- 상세 HTML 구조(어떤 태그/클래스에 회사명·제목·마감일 등이 들어있는지)는 curl로 받은 `/tmp/saramin_search.html`을 다시 받아서 실제로 파싱 로직을 만들어야 함 — 아직 셀렉터까지는 안 짰음, 여기서부터 시작.

## 7. 이직시스템 — README/AGENTS.md 갱신 + 커밋
- 현재 README의 "지금 단계에서는 사람인 공식 API만 사용한다. 로그인 우회, CAPTCHA 우회, 비공개 정보 수집은 하지 않는다." 문구를 다음 취지로 갱신: **공식 API(사람인·워크넷) 우선, 로그인/CAPTCHA 우회 없는 공개 검색결과 페이지 크롤링(사람인만, 잡코리아는 robots.txt로 제외) 을 보조 수단으로 병행한다.** 로그인 우회·CAPTCHA 우회·비공개 정보 수집은 여전히 하지 않는다는 원칙은 유지.
- `config.example.json`/`config.json`에 워크넷 관련 설정이 필요하면 추가(현재는 `queries`를 그대로 공유해서 쓰는 걸로 설계함 — 별도 워크넷 전용 검색어 리스트는 안 만듦).
- `WORK24_ACCESS_KEY` 환경변수 설정법을 README "1. 최초 설정"에 추가.
- `AGENTS.md`도 있으니 원칙 변경이 거기 반영돼야 하면 같이 수정.
- `verify_before_sync.sh`로 테스트·문법·비밀값 확인 후, 이직시스템 관련 파일만 `git add`해서 커밋·(확인 후) 푸시. `이직시스템/config.json`과 `data/`는 `.gitignore`에 이미 있어서 커밋 대상 아님.

## 9. shift_alarm — aut(github.com/likewoody/aut) 참고한 토큰 사용량 메뉴바 항목 (설계+검증 완료, shift_alarm.py 이식만 남음)

**배경**: 사용자가 aut(Swift 메뉴바 앱, Codex/Claude/Gemini 남은 quota 표시)를 보여주며 비슷한 기능을 shift_alarm에 추가해달라고 요청. Gemini CLI는 이 사용자 기기에 없어서(`~/.gemini` 없음) 제외, **Codex + Claude 둘 다 quota %까지** 구현하기로 확정(사용자가 "Codex + Claude quota %까지 다" 선택함).

**핵심 발견(aut Swift 소스 `macos/aut/ProviderDiagnostics.swift` 분석, 클론 위치: `/private/tmp/claude-501/-Users-forrestdpark/b9161bc8-619c-4435-9fc9-024ed3c63748/scratchpad/aut/`)**:
- **Codex**: `~/.codex/sessions/**/*.jsonl`에 Codex CLI가 자기 rate-limit을 **로컬 파일에 직접 기록**해둠(`event_msg` → `payload.type=="token_count"` → `payload.rate_limits`에 `{윈도우키: {used_percent, window_minutes, resets_at}}`). 네트워크·인증 불필요.
- **Claude 로컬 통계**(prompt-cache 적중률·user turns·model requests·모델명): `~/.claude/projects/**/*.jsonl`에서 `type=="assistant"`인 라인의 `message.usage.{input_tokens,cache_creation_input_tokens,cache_read_input_tokens}`로 계산.
- **Claude 남은 quota %**(로컬 로그엔 없음, 라이브 호출 필요): macOS 키체인의 `Claude Code-credentials`(Claude Code 자신이 이미 저장해둔 것) generic password를 읽으면 JSON `{"claudeAiOauth": {"accessToken": "..."}}` 형태이고, 그 accessToken으로 `GET https://api.anthropic.com/api/oauth/usage`를 호출하면(`Authorization: Bearer <token>`, `Accept: application/json`, `Content-Type: application/json`, `anthropic-beta: oauth-2025-04-20`, `User-Agent: claude-code/2.1.0`) `{윈도우키: {utilization: 0~100, resets_at: ISO날짜}}` 형태 JSON이 온다. 이건 비공개 엔드포인트지만 aut도 이 방식을 쓰고, Claude Code 자신이 이미 발급해둔 토큰을 자기 계정 조회에만 쓰는 것이라 문제 없다고 판단하고 진행함(사용자도 이미 이 방식으로 진행하기로 명시적으로 동의함).

**이미 작성·실제 테스트까지 완료한 구현체**: `/private/tmp/claude-501/-Users-forrestdpark/b9161bc8-619c-4435-9fc9-024ed3c63748/scratchpad/ai_usage.py`
- `get_codex_quota()` — 가장 최근 codex 세션 파일에서 마지막 rate_limits 읽음. **실제 실행 결과 확인함: 지금 Codex가 `primary` 윈도우(10080분=1주일) `used_percent: 100.0`으로 실제 소진 상태인 것까지 정확히 잡아냄** — 오늘 세션 초반에 사용자가 겪은 "Codex 토큰 부족" 문제와 정확히 일치.
- `get_claude_local_stats()` — 실행 결과: model `claude-sonnet-5`, user_turns 225, model_requests 213, prompt_cache_percent 약 97.9%.
- `_claude_access_token()` + `get_claude_live_quota()` — 키체인에서 토큰 읽어서 라이브 API 호출. **실행 결과 확인함**: `five_hour` 41% 사용, `seven_day` 6% 사용. **주의**: 이 함수를 부를 때 토큰 값 자체를 절대 print/log 하지 말 것(한 번 테스트 삼아 `security find-generic-password ... -w`를 그대로 출력하려다 하네스가 "Credential Materialization" 사유로 막았음 — 정상적인 차단이었음). 토큰은 항상 변수에만 담아서 바로 Authorization 헤더로 흘려보낼 것, 절대 print/파일저장/알림 문자열에 넣지 말 것.

**shift_alarm.py에 이식할 때 해야 할 일**:
1. `ai_usage.py`의 4개 함수(`get_codex_quota`, `get_claude_local_stats`, `_claude_access_token`, `get_claude_live_quota`)를 shift_alarm.py에 그대로(또는 같은 폴더에 `ai_usage.py`로 분리해서 import) 이식.
2. 메뉴에 새 섹션 추가(예: `🪙 Codex: 5시간 XX% · 주간 XX%`, `🪙 Claude: 5시간 XX% · 주간 XX%` 형태의 드롭다운 항목, 또는 하위 메뉴로 묶기).
3. 갱신 주기는 날씨(`_refresh_weather`, 별도 스레드)와 같은 패턴으로 — 너무 자주 API를 부르지 않게 예: 10~15분 간격 타이머 + 앱 시작 시 1회.
4. 값을 못 가져오면(quota API 실패, 키체인 없음, 세션 파일 없음 등) `aut`처럼 추측하지 말고 "확인 불가"로 표시 — 0%나 임의값으로 채우지 말 것.
5. 메뉴바 타이틀 자체(짧게 유지해야 하는 제약, `_update_title()`)에는 넣지 않고 드롭다운 메뉴 항목으로만 노출하는 걸 권장(타이틀이 너무 길어지면 macOS가 숨겨버리는 기존 제약이 있음 — 파일 상단 주석 참고).
6. shift_alarm/README.md에도 이 기능 설명 추가(데이터 출처, 갱신 주기, 실패 시 표시 등).

## 8. 참고 — 세션 전체에서 있었던 일 요약
- 오늘 세션에서 이미 완료·푸시된 것(새 세션에서 다시 안 해도 됨): shift_alarm의 kr서브도메인 리다이렉트 감지 버그 수정+추천사이트 메뉴 구분선(커밋 `bb8eda2`), 일본어자막추출의 Codex→Claude 자동 폴백 `ai_exec.py`(커밋 `f03c501`).
- 사람인 API 승인은 아직 결론 안 남(거절 확정 아님, 계속 대기 중이라고 보면 됨).
- 사용자는 한국어 대화에서 항상 존댓말을 선호함, shift_alarm.py 수정 완료 시 항상 자동으로 launchctl kickstart 재시작함, shift_alarm/README.md는 git이 정본이라 작업 후 항상 README도 같이 최신화·커밋함 — 이 세 가지는 기존 메모리에 저장돼 있어 자동 적용될 것.
